# 船舶沉没遗址层位关系复核台 — 项目设计文档

## 1. 目标

为水下考古人员提供一个可运行的层位关系复核工具：导入构件（component）与沉积层（sediment）
之间的接触关系，判断每个构件属于「原始沉没层 / 后期侵扰层 / 测绘误连」，构建**层位偏序**，
自动检测**不可能循环（矛盾）**并生成**侵扰候选**，支持研究者复核误连、裁决层位、发布不可变
的**剖面版本**。

核心领域问题：给定一组「上覆/切割」有向边（均表示上覆单元晚于下伏单元），求所有单元的相对
先后偏序；当边集产生环时，环内连接度最高的单元最可能是后期侵扰层或测绘误连，需要人工裁决。

## 2. 端到端闭环

```
导入接触关系(测绘边, 指纹幂等)
   → 研究者确认/否决误连(confirm/reject)
   → 重新求解偏序(reconcile)
   → 检测到环 → 标记矛盾接触(conflict) + 写矛盾记录 + 生成侵扰候选
   → 研究者裁决侵扰候选(accepted=扰动 / dismissed=排除)
   → 发布剖面版本(draft) → 共享(shared) → 冻结(frozen, 不可变)
```

`cmd/shipstrata --smoke-test` 真实执行上述闭环并验证「关闭重开同库」后数据仍在，成功退出 0。

## 3. 状态机（4 个，领域专有，非 CRUD 换皮）

| 实体 | 状态流转 | 不变量 |
|---|---|---|
| 遗址批次 site_batch | collecting → pending_review → published → sealed | sealed 后禁止任何写操作 |
| 地层单元 strata_unit | candidate → stable / disturbed / excluded | 侵扰裁决写入 disturbed/excluded |
| 接触关系 contact | pending → overlies / cuts / conflict / confirmed / excluded | 同一测绘边指纹幂等；confirm 后参与偏序 |
| 剖面版本 profile | draft → shared → frozen → superseded | frozen 不可变，保留原始快照 |

## 4. 持久化与恢复

- 驱动：`modernc.org/sqlite`（纯 Go，CGO 无关，`CGO_ENABLED=0`）。
- 建表：`site_batches` / `strata_units` / `contacts`(fingerprint UNIQUE) /
  `sample_points` / `profile_versions` / `contradictions` / `intrusion_candidates`，
  全部幂等 `CREATE TABLE IF NOT EXISTS` + 索引。
- 指纹幂等：每条接触关系按 `(from,to,relation,survey_source,survey_seq)` 计算 SHA-256 指纹；
  重复导入走 `INSERT OR IGNORE`，返回 `added=false` 与已有记录。
- 重启恢复：进程退出后重新 `OpenStore` 同一路径（WAL 模式），实体、矛盾、侵扰候选、剖面快照
  全部可从库读回（`--smoke-test` 已验证）。
- 并发：连接池 `MaxOpenConns=4`，启用外键与 `busy_timeout(5000)`，写入串行化不丢数据。

## 5. API 责任清单（`/api` 前缀，共 28 个端点）

| 方法 | 路径 | 责任 |
|---|---|---|
| POST | /api/sites | 创建遗址批次(collecting) |
| GET | /api/sites | 列出遗址 |
| GET | /api/sites/{id} | 获取遗址 |
| POST | /api/sites/{id}/advance | 推进状态机一步 |
| POST | /api/sites/{id}/seal | 封存(禁止再改) |
| POST | /api/units?site_id= | 创建地层单元(candidate) |
| GET | /api/units?site_id= | 列出单元 |
| GET | /api/units/{id} | 获取单元 |
| POST | /api/contacts?site_id= | 导入接触关系(指纹幂等) |
| POST | /api/contacts/batch?site_id= | 批量导入 |
| GET | /api/contacts?site_id= | 列出接触 |
| GET | /api/contacts/{id} | 获取接触 |
| POST | /api/contacts/{id}/confirm | 确认纳入偏序(触发重算) |
| POST | /api/contacts/{id}/reject | 否决误连(排除) |
| GET | /api/contradictions?site_id= | 列出矛盾记录 |
| POST | /api/intrusion/detect?site_id= | 重算并刷新侵扰候选 |
| GET | /api/intrusion/candidates?site_id= | 列出侵扰候选 |
| POST | /api/intrusion/{unitId}/adjudicate?site_id= | 裁决(accepted/dismissed) |
| POST | /api/profiles?site_id= | 发布剖面(draft, 含快照) |
| GET | /api/profiles?site_id= | 列出剖面(含被替代) |
| GET | /api/profiles/{id} | 获取剖面+快照 |
| POST | /api/profiles/{id}/share | 共享 |
| POST | /api/profiles/{id}/freeze | 冻结(不可变) |
| POST | /api/profiles/{id}/supersede | 替代旧版本 |
| POST | /api/samples?site_id= | 创建采样点证据 |
| GET | /api/samples?site_id= | 列出采样点 |
| GET | /api/graph?site_id= | 偏序求解(含矛盾/侵扰) |
| GET | /api/stats | 聚合统计 |
| GET | /api/self-check | 健康自检(store+solver) |

## 6. 不变量（运行时保证）

1. 自环接触被拒绝（`ErrSelfLoop`）。
2. 未知/跨遗址单元引用被拒绝（`ErrUnknownUnit`）。
3. 重复测绘边指纹幂等，不重复写入。
4. 矛盾接触自动标记 `conflict`；侵扰候选幂等刷新（删 open 后重建）。
5. `sealed` 遗址禁止发布新剖面（`ErrSiteSealed`）。
6. `frozen` 剖面不可变（快照固定，仅可 supersede）。

## 7. 端到端场景（已自测）

- 建遗址 `Wreck Y` → 建 3 单元（Hull/Ballast/Seabed）。
- 导入 3 条 `overlies` 边构成环 `Hull>Ballast>Seabed>Hull` → 确认全部。
- 求解检测到环：3 条接触标记 `conflict`，写入 1 条矛盾记录，生成 1 个侵扰候选（环内连接度
  最高的 Ballast）。
- 裁决该候选 `dismissed` → 单元置 `excluded`，重新求解。
- 发布剖面 `v1`(draft) → 冻结(frozen)。
- `stats` 返回 `units:3, contacts:3, contradictions:1, intrusion_candidates:1, profiles:1`。
- HTTP 集成测试与 `--smoke-test` 均通过；`go build`/`go vet`/`go test` 全部成功。
